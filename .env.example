# Loa'a Environment Configuration
# Copy this file to .env and customize for your environment
# (Note: devenv.nix sets these automatically for development)

# Database Configuration
# Options: memory, embedded, remote
LOAA_DB_MODE=embedded

# Database path (for embedded mode)
# Use relative path from project root (no absolute paths!)
LOAA_DB_PATH=./data/loaa.db

# Database URL (for remote mode)
# LOAA_DB_URL=127.0.0.1:8000

# MCP Server Configuration
# Options: stdio, http
# LOAA_MCP_TRANSPORT=stdio

# HTTP transport port (when using http transport)
# LOAA_MCP_HTTP_PORT=3001

# Server Configuration
# LOAA_SERVER_HOST=127.0.0.1
# LOAA_SERVER_PORT=3000

# All-in-one Mode (web + MCP in single process)
# Set to true to run MCP server alongside web server
# LOAA_INCLUDE_MCP=true

# MCP Server Configuration (for all-in-one or standalone mode)
# LOAA_MCP_HOST=127.0.0.1
# LOAA_MCP_PORT=3001

# OAuth Configuration
# JWT signing secret (REQUIRED for production)
# Generate with: openssl rand -base64 32
# IMPORTANT: Both web and MCP servers must use the same secret
LOAA_JWT_SECRET=insecure-default-change-me

# Base URL for OAuth redirects
# LOAA_BASE_URL=http://127.0.0.1:3000
